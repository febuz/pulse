# Pulse Peer

The peer runtime is the process that speaks Pulse.

It should be small enough to run on a laptop, home server, hosted VM, or local community node.

Initial responsibilities:

- load peer identity
- store local web objects
- connect to known peers
- use relay when direct connectivity fails
- expose a local API for apps

